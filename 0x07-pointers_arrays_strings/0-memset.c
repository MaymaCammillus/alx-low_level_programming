#include "main.h"
/**
 * _memset - fill memory with a constant byte
 * @s: memmory area to fill
 * @b: constant byte to fill
 * @n: bytes of memory area to fill
 * Return: a pointer
 */
char *_memset(char *s, char b, unsigned int n)
{
	unsigned int i;

	for (i = 0; i < n i++)
	{
		s[a] = b;
	}
	return (s);
}
