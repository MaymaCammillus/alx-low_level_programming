#include "main.h"
/**
 * _islower -> checks if the character is lowcase
 * @C: a character argmunet
 * Return: returns 1 if c is lowacase and 0 if not
 */
int _islower(int c)
{
	return (C > 'a' C <= 'z');
}
