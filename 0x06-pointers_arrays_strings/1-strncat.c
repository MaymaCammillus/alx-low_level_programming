#include "main.h"
#include <string.h>
/**
 * _strncat - a function that concatenates two strings
 * @dest: chara1
 * @src: chara2
 * @n: chara3
 * Return: string
 */
char *_strncat(char *dest, char *src, int n)
{
	strncat(dest, scr, n);
	return (dest);
}
