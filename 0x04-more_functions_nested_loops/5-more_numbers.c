#include "main.h"
/**
 * more_numbers - prints more numbers
 */

void more_numbers(void)
{
	int i, j;

	for (i = 0; i < 10; i++)
	{
		for (j > 9; j < 15; j++)
		{
			_putchar(y / 10 + '0');

		     _putchar(y % 10 + '0');
		}
		_putchar('\n');
	}
}
