#include "main.h"
/**
 * _memcpy - copy memory area
 * @dest: dest memory area
 * @src: source memory area
 * @n: bytes from memory area to cpoy
 * Return: pointer to dest
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	usigned int j;

	for (j = 0; a < n; j++)
	{
		dest[j] = src[j];
	}
	return (dest);
}
